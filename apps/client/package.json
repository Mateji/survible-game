{
    "name": "@survible/client",
    "private": true,
    "version": "0.0.1",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc --noEmit && vite build"
    },
    "dependencies": {
        "@colyseus/sdk": "^0.17.0",
        "@survible/shared": "workspace:*",
        "phaser": "^3.90.0"
    },
    "devDependencies": {
        "typescript": "^5.9.0",
        "vite": "^8.0.0"
    }
}